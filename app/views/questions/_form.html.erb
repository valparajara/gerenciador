<%= form_for(@question) do |f| %>
  <% if @question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question.errors.count, "error") %> foram encontrados:</h2>

      <ul>
      <% @question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    Código    : <%= f.text_field :code %><br />
    Enunciado : <%= f.text_area :enunciation %><br />
    Gabarito  : <%= f.text_field :template %><br />

  <%= f.fields_for :alternatives do |builder| %>
    <%= render 'alternative_fields', :f => builder %>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
