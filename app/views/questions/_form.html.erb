<%= form_for(@question) do |f| %>
  <% if @question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question.errors.count, "error") %> foram encontrados:</h2>

      <ul>
      <% @question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <p>Código    : <%= f.text_field :code %></p>
    <p>Enunciado : <%= f.text_area :enunciation %></p>
    <p>Gabarito  : <%= f.text_field :template %></p>

  <%= f.fields_for :alternatives do |builder| %>
    <%= render 'alternative_fields', :f => builder %>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
